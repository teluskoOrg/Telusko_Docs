import React, { type ReactNode } from 'react'

type Button = {
    text: string,
    icon?: ReactNode | string
}

function Button() {
  return (
    <div>
        
    </div>
  )
}

export default Button